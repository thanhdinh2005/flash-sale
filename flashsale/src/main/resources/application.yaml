spring:
  application:
    name: flashsale

  profiles:
    active: dev

application:
  security:
    jwt:
      secret-key: ${SECRET_KEY}
      expiration: ${TOKEN_EXP}
      refresh-expiration-ms: ${REFRESH_EXP}

server:
  port: ${APP_PORT:8081}
  servlet:
    context-path: /api/v1